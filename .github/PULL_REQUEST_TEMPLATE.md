## Summary

- 

## Test plan

- [ ] Backend: `npm run lint` and `npm test` (with `TEST_DATABASE_URL`)
- [ ] Frontend: `npm run lint`, `npm test`, `npm run tsc`, `npm run build`
- [ ] Manual smoke (if UI/auth changed): login, demo read-only, admin path as relevant

## Notes

- Docs / `.env.example` updated when behavior or environment variables change
- Security-sensitive changes follow [SECURITY.md](../SECURITY.md)
